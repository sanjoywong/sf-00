{% extends 'base.html.twig' %}

{% block title %}Categorie2 {% endblock %}

{% block body %}
    <h1>Categorie </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie2 in categorie2s %}
            <tr>
                <td>{{ categorie2.id }}</td>
                <td>{{ categorie2.titre }}</td>
                <td>
                    <a href="{{ path('app_categorie2_show', {'id': categorie2.id}) }}">show</a>
                    <a href="{{ path('app_categorie2_edit', {'id': categorie2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie2_new') }}">Create new</a>
{% endblock %}
