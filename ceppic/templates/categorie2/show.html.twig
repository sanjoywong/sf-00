{% extends 'base.html.twig' %}

{% block title %}Categorie2{% endblock %}

{% block body %}
    <h1>Categorie2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie2.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categorie2.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie2_index') }}">back to list</a>

    <a href="{{ path('app_categorie2_edit', {'id': categorie2.id}) }}">edit</a>

    {{ include('categorie2/_delete_form.html.twig') }}
{% endblock %}
